epochs: 1000
learning_rate: 1e-3
weight_decay: 0.0

optimizer:
  type: "adam"
  betas: [0.9, 0.999]

log_every_n_steps: 10
sample_every_n_epochs: 50
save_every_n_epochs: 100

vis_n_samples: 500
vis_n_trajectories: 10
