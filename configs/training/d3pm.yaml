epochs: 100
learning_rate: 3e-4
weight_decay: 0.01

optimizer:
  type: "adamw"
  betas: [0.9, 0.999]

scheduler:
  use_scheduler: true
  type: "cosine"
  warmup_epochs: 5

log_every_n_steps: 50
sample_every_n_epochs: 5
save_every_n_epochs: 10

vis_n_samples: 5
vis_seq_len: 64
vis_n_steps: 50
