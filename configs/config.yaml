defaults:
  - model: ddpm_2d
  - data: two_clusters
  - training: default
  - _self_

experiment_name: "ddpm_2d_baseline"
seed: 42

device: "cpu"

tensorboard:
  log_dir: "outputs/logs"

checkpoint_dir: "outputs/checkpoints"
sample_dir: "outputs/samples"
